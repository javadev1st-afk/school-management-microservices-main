server:
  port: 8003
  servlet:
    context-path: /

spring:
  application:
    name: payment-service
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/school_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 20000

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

app:
  base-url: http://192.168.1.10:8000/ui
  jwt-secret-key: mySecretKeyIsMoreSecureThanAnyOtherSecureApplicatoinOrKeyStore
  corsallowed:
    - "http://localhost:4200"
    - "http://192.168.1.10:8000"
  open-endpoints:
    - "/ui/**"
    - "/api/auth/**"
    - "/api/public/**"
    - "/internal/**"
    - "/v3/api-docs/**"
    - "/swagger-ui/**"
    - "/swagger-ui/index.html"
    - "/static/ui/**"
  jwt-expiration-ms: 86400000
  payment:
    reminder:
      days-before-due: 3
      days-after-due: 7
      days-after-due-14: 14

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  api-docs:
    path: /v3/api-docs